server.port=8081
spring.datasource.url=jdbc:postgresql://localhost:5432/locationdb
spring.datasource.username=postgres
spring.datasource.password=Hatdogako.22
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT config for local auth - this whole section gets removed when we switch to Azure Entra ID
# The secret is just a local dev key, it's not used in production
app.jwt.secret=local-dev-secret-replace-this-before-production
# Token expires after 24 hours
app.jwt.expiration-ms=86400000