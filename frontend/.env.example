# Copy this file to .env and fill in your own values.
# .env is gitignored — never commit it.

# Deployed Azure App Service (use this for dev builds)
EXPO_PUBLIC_API_BASE_URL=https://locationapp-gqejafhbc0h4hqg9.westeurope-01.azurewebsites.net
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=YOUR_GOOGLE_PLACES_API_KEY

# Local fallback — only needed if running the backend yourself
# EXPO_PUBLIC_API_BASE_URL=http://YOUR_LOCAL_IP:8080
